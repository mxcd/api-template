# Migration `20200903101658-init`

This migration has been generated by Max Partenfelder at 9/3/2020, 12:16:58 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Group" (
"id" SERIAL,
"name" text   NOT NULL ,
"displayName" text   NOT NULL ,
"createdBy" text   NOT NULL ,
"createdDate" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"modifiedBy" text   NOT NULL ,
"modifiedDate" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Member" (
"id" SERIAL,
"firstName" text   NOT NULL ,
"lastName" text   NOT NULL ,
"username" text   NOT NULL ,
"email" text   NOT NULL ,
"pictureId" integer   ,
"workPackage" text   ,
"createdBy" text   NOT NULL ,
"createdDate" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"modifiedBy" text   NOT NULL ,
"modifiedDate" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."_GroupToMember" (
"A" integer   NOT NULL ,
"B" integer   NOT NULL 
)

CREATE UNIQUE INDEX "Group.name_unique" ON "public"."Group"("name")

CREATE UNIQUE INDEX "_GroupToMember_AB_unique" ON "public"."_GroupToMember"("A", "B")

CREATE INDEX "_GroupToMember_B_index" ON "public"."_GroupToMember"("B")

ALTER TABLE "public"."_GroupToMember" ADD FOREIGN KEY ("A")REFERENCES "public"."Group"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_GroupToMember" ADD FOREIGN KEY ("B")REFERENCES "public"."Member"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200903101658-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,34 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+model Group {
+  id           Int      @default(autoincrement()) @id
+  name         String   @unique
+  displayName  String
+  createdBy    String
+  createdDate  DateTime @default(now())
+  modifiedBy   String
+  modifiedDate DateTime @default(now())
+  members      Member[]
+}
+
+model Member {
+  id           Int      @default(autoincrement()) @id
+  firstName    String
+  lastName     String
+  username     String
+  email        String
+  pictureId    Int?
+  workPackage  String?
+  createdBy    String
+  createdDate  DateTime @default(now())
+  modifiedBy   String
+  modifiedDate DateTime @default(now())
+  groups       Group[]
+}
```


